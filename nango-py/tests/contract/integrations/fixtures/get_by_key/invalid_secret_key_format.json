{
    "scenario": "invalid_secret_key_format",
    "request": {
        "method": "GET",
        "path": "/integrations/github",
        "headers": { "Authorization": "Bearer not-a-uuid" },
        "query": {}
    },
    "seed": null,
    "response": {
        "status": 401,
        "body": {
            "error": {
                "message": "Authentication failed. The provided secret key is not a UUID v4.",
                "code": "invalid_secret_key_format",
                "payload": {}
            }
        }
    },
    "notes": "Bearer token fails UUID v4 regex check."
}
